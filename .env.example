# No repository path configuration is required.
# The agent submits an absolute target_path to repository_create/repository_clone.
# Existing repositories are selected by repository_id from the per-user JSON index.
